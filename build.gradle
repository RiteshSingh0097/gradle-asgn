

apply plugin : 'java'

dependencies {
    compile 'org.apache.commons:commons-lang3:3.6'// lang3 dependency Q4
}

// adding sources directory Q2
sourceSets {
    main {
        java {
            srcDirs = ['src/main/java', 'src/main/java2']
        }

        // Excluding file1.txt from jar  Q3
        resources{
            srcDirs = ['src/main/resources']
            exclude 'file1.txt'
        }
    }


}

//Creating jar file Q1
jar {
    manifest {
        attributes(
                'Main-Class': 'Hello'
        )
    }
}

//Question4

repositories {
    mavenCentral()
}

// Question5

repositories {
    jcenter()
}

dependencies {
    compile 'org.apache.commons:commons-lang3:3.8.1'
}

jar{
    from configurations.compile.collect{ zipTree it }
}

//Q6

apply from : 'myTask.gradle'